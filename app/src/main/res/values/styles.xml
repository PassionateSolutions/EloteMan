<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowAnimationStyle">@style/SimpleTransitions</item>
    </style>

    <!--Transition Styles-->

    <style name="SimpleTransitions" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/slide_into_top</item>
        <item name="android:activityCloseEnterAnimation">@anim/slide_into_bottom</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_outo_left</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_outo_right</item>
    </style>


</resources>
